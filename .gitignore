node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
p.md
.env
.env.*
!.env.example
!.env.development.example
.nitro
.tanstack
.wrangler
.output
.vinxi
todos.json

.dev.vars*
!.dev.vars.example

.phase3-seal-backup/
.morph-builds
# Theme workspaces a locally-run Live Preview lays out. Source the transport
# wrote, not source anyone authored.
.morph-previews
.morph-previews-*
*Zone.Identifier*
*:Zone.Identifier:
# Playwright
e2e/.auth/
test-results/
playwright-report/
.env.e2e

# Local pnpm content-addressable store. Package bytes, not source: 19MB of it
# reached this repo once already, and it grows with every install.
.pnpm-store/

# Per-machine working notes from the AI assistant. Local observations about this
# checkout, not source. `.workbuddy-ai/skills/` is deliberately left trackable:
# project-level skills are meant to be shared with the team.
.workbuddy-ai/memory/
