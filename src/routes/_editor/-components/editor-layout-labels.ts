/**
 * Shared layout copy used by the tree, inspector, and navigation surfaces.
 *
 * Storage ids such as `starter-header` stay internal. These strings describe
 * the thing an author is editing and the scope of that edit.
 */
export const GLOBAL_LAYOUT_LABEL = "Global";

export const SHARED_LAYOUT_HINT =
  "Shared by every page — editing this changes them all";
