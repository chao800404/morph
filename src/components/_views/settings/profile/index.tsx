

import { CardRenderer } from "../../../_components/card/card-renderer";
import { PROFILE_CONFIG } from "./_config/profile.config";

const Profile = async () => {
    // const session = await getSession();
    // if (!session) return null;

    // return (
    //     <section className="space-y-4 p-3">
    //         {PROFILE_CONFIG.sections.map(section => (
    //             <CardRenderer key={section.slug} section={section} session={session} />
    //         ))}
    //     </section>
    // );
};

export default Profile;
